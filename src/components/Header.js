import "../stylesheets/Header.scss";

const Header = () => {
    return (
        <header>
            <div className="logo-container">
                <img src="logo734.png" alt="logo" />
                <h1>YouTube</h1>
            </div>
        </header>
    );
};

export default Header;
